echo "Executing launch_settings.cfg..."

// Here are settings that I only want to be set once when I launch the game.

volume 0.1175
cl_disablehtmlmotd 1

voice_enable 1
alias voice_enable "echo Use voice_modenable instead of voice_enable."
voice_modenable 1
voice_scale 1

// Load up our custom crosshair so it works on sv_pure 2 servers
//sv_allow_wait_command 1
//map itemtest; wait 1000; jointeam blu; wait 300; join_class sniper; wait 300; disconnect
//exec sv_pure_0_crosshair
//map custom_crosshair_loader
exec sensitivities

echo "Finished executing launch_settings.cfg."